import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const LoginResetScreen = (props: Props) => {
  return (
    <View>
      <Text>LoginResetScreen</Text>
    </View>
  );
};

export default LoginResetScreen;

const styles = StyleSheet.create({});
