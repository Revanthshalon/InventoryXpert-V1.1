import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const ExpenditureAnalysisScreen = (props: Props) => {
  return (
    <View>
      <Text>ExpenditureAnalysisScreen</Text>
    </View>
  );
};

export default ExpenditureAnalysisScreen;

const styles = StyleSheet.create({});
